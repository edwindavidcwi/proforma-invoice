// Top-level build file. Plugin versions are resolved from Google's Maven
// (google()), which Android Studio downloads automatically.
plugins {
    id 'com.android.application' version '8.2.2' apply false
}
